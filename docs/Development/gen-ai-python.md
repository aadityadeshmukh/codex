# Generative AI with Python

Setting up a TensorFlow lab

??? info
    - The main components of a Tensorflow lab:
        ``` mermaid
            graph TB
                A[ IDE ];
                B[Docker];
                C[Kubernetes];
                D[Kubeflow];
                E[Terraform];
                F[Cloud];
                G[Anaconda];
                H[Jupyter Notebook];
        ```